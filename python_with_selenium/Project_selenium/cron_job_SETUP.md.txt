1. Open Terminal -> crontab -e
2. select the friendly editor (1)
3. # m h  dom mon dow   command

Field          Description    Allowed Value
(m)MIN         Minute field    0 to 59
(h)HOUR        Hour field      0 to 23
(dom)DOM       Day of Month    1-31
(mon)MON       Month field     1-12
(dow)DOW       Day Of Week     0-6
(command)CMD      Command      Any command to be executed.

4. For Eg- 00 23 * * * cd ~/cpa_pm_test/pm
5. MAILTO=ashlesha14061998@gmail.com (to send mail after completion execution of task)
6. For sending mail must install ssmtp
    sudo apt-get install ssmtp
7. To config ssmtpcr
  sudo vim /etc/ssmtp/ssmtp.conf
8.Config file for sSMTP send mail and do the below changes

root=cpasage2022@gmail.com          
mailhub=smtp.gmail.com:587
AuthUser=cpasage2022@gmail.com
AuthPass=Cpasage@123             (User's mail id and password)
UseSTARTTLS=Yes
UseTLS=Yes
rewriteDomain=gmail.com
hostname=localhost              (to get local machine hostname = $hostname in terminal)
FromLineOverride=YES

9. For AUTHENTICATION error :
Open mail->settings
IMAP enabled
Allow less secure apps is ON
IMAP enabled
save changes
Allow less secure apps is ON

10. For WebDeriverException update the chrome driver
1. Check you have installed latest version of chrome brwoser-> "chromium-browser -version"
2. If not, install latest version of chrome "sudo apt-get install chromium-browser"
3. get appropriate version of chrome driver from following link http://chromedriver.storage.googleapis.com/index.html
4. Unzip the chromedriver.zip
5. Move the file to /usr/bin/ directory sudo mv chromedriver /usr/bin/
6. Goto /usr/bin/ directory and you would need to run something like "chmod a+x chromedriver" to mark it executable.
7. sudo `which webdrivermanager` chrome --linkpath /usr/local/bin (automatically update chrome driver)
10. Some useful commands

crontab -e/-l/-r
sudo service cron status
$ sudo grep --color -i cron /var/log/syslog
sudo vim /etc/ssmtp/ssmtp.conf
bash +x <scriptname>.sh
bash ./<scriptname>.sh

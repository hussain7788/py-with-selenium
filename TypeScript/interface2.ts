interface person {
    name: string;
    age: number;
    add: string;
    number: number;
}
interface employee {
    emp_degn: string;
    emp_salary: boolean;
    emp_id: string;
}

class teacher{
    teacherName:string;
    teacheradd
        constructor(p: person, e:employee){
            console.log(`my name is ${p.name} and my age is ${p.age} and address is ${p.add} and number is ${8179464351}`)
            console.log(`emp degn is ${e.emp_degn} and salary is ${e.emp_salary} and id is ${e.emp_id}`)
        }

}